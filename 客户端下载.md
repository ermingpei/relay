# RustDesk 客户端自动上报工具 - 下载指南

## 📥 快速下载

### Windows用户（中国）

**下载链接：**
```
https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.bat
```

**使用方法：**
1. 点击上面的链接
2. 右键 → "另存为" → 保存到桌面
3. 双击运行 `auto_report_ip_client.bat`
4. 完成！

### Mac/Linux用户

**下载链接：**
```
https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.sh
```

**使用方法：**
```bash
# 下载
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.sh

# 添加执行权限
chmod +x auto_report_ip_client.sh

# 运行并安装定时任务
./auto_report_ip_client.sh install
```

---

## 🎯 这个工具做什么？

### 全自动IP上报
- ✅ 自动检测你的公网IP
- ✅ 自动上报到服务器
- ✅ 自动添加到白名单
- ✅ 自动安装定时任务（每小时检查）
- ✅ 只在IP变化时上报（节省流量）

### 无需手动操作
- ❌ 不需要手动获取IP
- ❌ 不需要发送给管理员
- ❌ 不需要等待管理员添加
- ✅ 完全自动化！

---

## 📋 详细使用说明

### Windows版本

#### 第一次使用：

1. **下载文件**
   - 访问：https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.bat
   - 右键 → "另存为" → 保存到桌面

2. **运行文件**
   - 双击 `auto_report_ip_client.bat`
   - 会显示：
     ```
     ============================================
        RustDesk IP自动上报工具
        电脑名称: YOUR-COMPUTER
     ============================================
     
     [步骤1] 正在上报IP到服务器...
     
     正在获取公网IP...
     当前公网IP: 36.33.125.192
     
     正在发送IP到服务器...
     [成功] IP已上报到服务器！
     
     ✅ IP已成功添加到服务器白名单！
     
     [步骤2] 检查定时任务...
     
     正在安装定时任务...
     [成功] 定时任务已安装
             - 每小时自动检查IP变化
             - 开机时自动运行
             - 只在IP变化时上报
     
     ============================================
        完成！
        - 当前IP: 36.33.125.192
        - 定时任务: 每小时自动检查
        - 日志文件: C:\Users\YourName\.rustdesk_ip_report.log
     ============================================
     ```

3. **完成！**
   - 以后每小时自动检查
   - 开机时自动运行
   - IP变化时自动上报

#### 如果需要管理员权限：

如果看到"需要管理员权限"提示：
1. 右键 `auto_report_ip_client.bat`
2. 选择"以管理员身份运行"
3. 点击"是"

---

### Mac/Linux版本

#### 第一次使用：

```bash
# 1. 下载文件
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.sh

# 2. 添加执行权限
chmod +x auto_report_ip_client.sh

# 3. 运行并安装定时任务
./auto_report_ip_client.sh install
```

会显示：
```
============================================
   RustDesk IP自动上报工具
   设备名称: MacBook-Pro
============================================

正在获取公网IP...
当前公网IP: 36.33.125.192

正在发送IP到服务器...
✅ IP已上报到服务器！

安装定时任务...
✅ 定时任务已安装
   - 每小时自动检查IP变化
   - 只在IP变化时上报
```

#### 查看状态：

```bash
./auto_report_ip_client.sh status
```

#### 卸载定时任务：

```bash
./auto_report_ip_client.sh uninstall
```

---

## 🔍 如何验证是否工作？

### Windows：

1. 打开文件浏览器
2. 在地址栏输入：`%USERPROFILE%`
3. 找到文件：`.rustdesk_ip_report.log`
4. 双击打开，查看日志

### Mac/Linux：

```bash
cat ~/.rustdesk_ip_report.log
```

应该看到类似：
```
[2025-01-04 10:30:15] SUCCESS: IP reported: 36.33.125.192
[2025-01-04 11:30:20] INFO: IP unchanged, skipped
[2025-01-04 12:30:25] INFO: IP unchanged, skipped
```

---

## ⚠️ 重要提示

### 服务器必须先启动

在客户端使用之前，管理员必须在服务器上运行：

```bash
./auto_update_ip_server.sh start
```

如果客户端显示"无法连接服务器"，请联系管理员确认服务器已启动。

### 防火墙设置

服务器需要开放端口8888：

```bash
# GCP防火墙规则
gcloud compute firewall-rules create rustdesk-auto-ip \
    --allow tcp:8888 \
    --source-ranges 0.0.0.0/0 \
    --target-tags myserver
```

---

## 🆘 常见问题

### Q: 显示"无法连接服务器"？

**A: 检查以下几点：**
1. 服务器是否运行了 `auto_update_ip_server.sh start`
2. 服务器防火墙是否开放端口8888
3. 网络连接是否正常

**解决方法：**
- 联系管理员确认服务器状态
- 或者使用手动方法：运行 `getip.bat` 获取IP，发给管理员

### Q: 定时任务安装失败？

**A: Windows需要管理员权限**
- 右键文件 → "以管理员身份运行"

**A: Mac/Linux检查cron**
```bash
crontab -l
```

### Q: 如何知道IP已经添加成功？

**A: 查看日志文件**
- Windows: `%USERPROFILE%\.rustdesk_ip_report.log`
- Mac/Linux: `~/.rustdesk_ip_report.log`

看到 `SUCCESS: IP reported` 就是成功了。

### Q: 可以手动运行吗？

**A: 可以！**
- Windows: 双击 `auto_report_ip_client.bat`
- Mac/Linux: `./auto_report_ip_client.sh`

### Q: 如何卸载？

**A: Windows**
1. 打开"任务计划程序"
2. 找到"RustDesk Auto IP Report"
3. 右键 → 删除

**A: Mac/Linux**
```bash
./auto_report_ip_client.sh uninstall
```

---

## 📊 对比：自动 vs 手动

| 特性 | 自动上报 | 手动方法 |
|------|---------|---------|
| **获取IP** | ✅ 自动 | ❌ 手动运行getip.bat |
| **上报IP** | ✅ 自动 | ❌ 手动发给管理员 |
| **添加白名单** | ✅ 自动 | ❌ 管理员手动添加 |
| **定时检查** | ✅ 每小时 | ❌ 需要时手动 |
| **IP变化检测** | ✅ 自动 | ❌ 手动判断 |
| **开机自动** | ✅ 是 | ❌ 否 |
| **需要管理员** | ❌ 不需要 | ✅ 需要 |

**推荐使用自动上报！** 🎉

---

## 🔐 安全说明

### 密钥保护
- 脚本使用密钥验证身份
- 只有正确的密钥才能添加IP
- 密钥已内置在脚本中

### 频率限制
- 服务器限制每个IP每小时最多100次请求
- 防止滥用

### IP验证
- 只接受有效的公网IP
- 拒绝私有IP（192.168.x.x等）

---

## 📞 获取帮助

如果遇到问题：

1. **查看日志**
   - Windows: `%USERPROFILE%\.rustdesk_ip_report.log`
   - Mac/Linux: `~/.rustdesk_ip_report.log`

2. **联系管理员**
   - 提供日志内容
   - 说明错误信息

3. **使用手动方法**
   - 如果自动方法不工作
   - 可以临时使用 `getip.bat` 手动获取IP

---

## 🎉 开始使用

### Windows（最简单）：

1. 下载：https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.bat
2. 双击运行
3. 完成！

### Mac/Linux：

```bash
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/auto_report_ip_client.sh
chmod +x auto_report_ip_client.sh
./auto_report_ip_client.sh install
```

**就这么简单！** ✨
